/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/Portfolio-26',
  assetPrefix: '/Portfolio-26',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
