/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/souvik-mandol',
  assetPrefix: '/souvik-mandol',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
